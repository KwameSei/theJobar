import styled from '@emotion/styled'
import { Box } from '@mui/material'

// custom css for mui components
const cssMUI = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
})

export default cssMUI;